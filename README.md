# Pam Premium Dashboard (encrypted)

Password-protected hotel dashboard. The `index.html` here contains only AES-256-GCM
encrypted content - it is decrypted in the browser with the password.
Auto-published by the HMS dashboard pipeline.
